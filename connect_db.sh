# Antes de esto ejecutar docker compose up -d

docker exec -it solidarity_mysql mysql -u root -proot -D solidarityprueba

# Despues ejecutar docker compose down (cuando se quiera eliminar el contenedor)